@{
    string user = Context.Session.GetString("Username");
}
<nav>
    <div class="d-flex justify-content-between m-4">
        <div>
            <h1>Welcome @user</h1>
        </div>
        <div class="d-flex justify-content-evenly gap-5 align-items-center">
            <a asp-action="Weddings" asp-controller="Wedding">Home</a>
            <a asp-action="WeddingForm" asp-controller="Wedding" class="Classes">Plan a Wedding</a>
            <form asp-action="LogOut" asp-controller="User" method="post">
                <button class="btn btn-danger">Log out</button>
            </form>
        </div>
    </div>
</nav>